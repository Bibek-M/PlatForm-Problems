# 194. Namaste Codehelp

🟡 **Difficulty:** Easy

---

## 🧠 Problem Statement

In this problem, you need to display a specific message using a programming language of your choice. The message to output is "Namaste Codehelp", but with particular formatting. You need to encapsulate the message with the prefix and suffix "@@". That is, the output should be "@@Namaste Codehelp@@". This problem helps you practice basic string formatting and output operations. Ensure the output string is exactly as specified, with attention to capitalization and spacing.

## Example
Here is what the output should look like:


 ```cpp
 @@Namaste Codehelp@@
 ```
As shown, the text "Namaste Codehelp" is enclosed with "@@" at both the start and the end, exactly matching the required format.

---

## 📌 Examples

### 🔹 Example 1

```
Input:

name = Codehelp

Output:

Namaste Codehelp
Explanation:The program should output the exact phrase as shown without any extra spaces or new lines.
```

---

## ⚙️ Constraints

- `There are no input constraints, as this problem only requires printing a single line of text.`
