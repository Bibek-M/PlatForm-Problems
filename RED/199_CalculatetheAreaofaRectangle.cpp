class Solution {
public:
    long long calculateArea(int length, int breadth) {
        // Implement the logic to calculate area of a rectangle
        long long area = (long long)length*(long long)breadth;
        return area;
        // return (long) length*breadth;
    }
};